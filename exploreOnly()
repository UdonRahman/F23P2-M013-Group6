## Explore Only 

# Generate happiness for cafeteria 1 using it’s mean (H1) and # standard deviation (D1)
import random

m1 = [7, 4, 10, 5]
d1 = [3, 10, 6, 2]


#generating random happiness
def h(p1:int,p2:int)->int: # get a random happiness
    happy = random.normalvariate(p1,p2)
    return happy



# 
def exploreonly(p1:list, p2:list)->list: 
# Simulation of visiting each cafeteria for 50 days
    result = 0
    for cafeteria in range(len(p1)):
        days = 50
        while days > 0:
            h2 = h(p1[cafeteria],p2[cafeteria])
            days = days -1
            result = result + h2
    
    return result

# Testing out the function
print(exploreonly(m1, d1))


print("Total happiness over 200 days:", total_happiness)
